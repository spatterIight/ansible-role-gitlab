# SPDX-FileCopyrightText: 2026 Slavi Pantaleev
# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
name: molecule

on:  # yamllint disable-line rule:truthy
  push:
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "LICENSE"
      - "LICENSES/**"
      - "REUSE.toml"
  pull_request:
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "LICENSE"
      - "LICENSES/**"
      - "REUSE.toml"
  workflow_dispatch:

jobs:
  molecule:
    name: Run Molecule scenario '${{ matrix.scenario }}' on ${{ matrix.distro }}
    runs-on: ubuntu-latest

    # Same-repository pull requests already run via the push event;
    # run pull_request jobs only for pull requests from forks.
    if: >-
      github.event_name != 'pull_request'
      || github.event.pull_request.head.repo.full_name != github.repository

    strategy:
      matrix:
        distro:
          - ubuntu2604
        scenario:
          - default
          - postgres
          - postgres-valkey
      fail-fast: false

    env:
      MOLECULE_DISTRO: ${{ matrix.distro }}
      PY_COLORS: "1"
      ANSIBLE_FORCE_COLOR: "1"

    steps:
      # GitLab's container image unpacks to ~5.5 GB, and the Docker daemon inside the Molecule
      # container keeps the downloaded layers besides that. To leave room for both, the runner's
      # preinstalled toolchains which this role does not use are removed first.
      - name: Free up disk space
        run: sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL

      - name: Checkout repository
        uses: actions/checkout@v7.0.0
        with:
          path: gitlab

      - name: Set up Python
        uses: actions/setup-python@v6.3.0
        with:
          python-version-file: gitlab/.python-version
          cache: pip
          cache-dependency-path: gitlab/molecule/requirements.txt

      - name: Install Python dependencies
        working-directory: gitlab
        run: pip install -r molecule/requirements.txt

      - name: Cache Ansible collections
        uses: actions/cache@v6.1.0
        with:
          path: ~/.ansible/collections
          key: ansible-collections-${{ hashFiles('gitlab/molecule/*/requirements.yml') }}

      - name: Run Molecule
        working-directory: gitlab
        run: molecule test --scenario-name ${{ matrix.scenario }}
